defaults:
  - design_space: node_clf
  - resource: cpu-4
  - mlflow: local
  - _self_

sample_size: 128
random_seed: 42
gated_datasets:
  - mouse-kidney   # ~17 GB file, ~51 GB peak during torch.load()
  - breast-cancer  # ~6 GB file,  ~18 GB peak during torch.load()
