defaults:
  - full
  - _self_

model:
  global_pooling: ['mean', 'max']
task:
  type: 'graph-classification'
  _zip:
    - dataset_name
    - num_classes
  dataset_name:
    - brca
  num_classes:
    - 1
